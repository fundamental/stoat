language: cpp
compiler:
    - clang
before_install:
    - sudo apt-get install -qq llvm-dev
    - sudo apt-get install -qq ruby-graphviz
script:
    - mkdir build
    - cd build
    - cmake ..
    - make
    - make test
